import * as types from './../actions/ActionTypes';

const initialState = {}

function blog(state = initialState, action){
  switch(action.type){
    default:
      return state;
  }
}

export default blog;
